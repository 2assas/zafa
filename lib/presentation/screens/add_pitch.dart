import 'package:flutter/material.dart';

class AddPitch extends StatelessWidget {
  const AddPitch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is add pitch screen"),
    );
  }
}
